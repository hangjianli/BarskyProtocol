# Implement One BPE Merge Step

Implement `merge_once(word, bigram)` where:
- `word` is a tuple of symbols, e.g. ("l", "o", "w")
- `bigram` is a pair to merge, e.g. ("l", "o")
Return a new tuple where every adjacent occurrence of `bigram` is merged into one symbol.
